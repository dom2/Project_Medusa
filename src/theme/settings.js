const red = '#F80000';
const black = '#000';
const slate = '#324B5C';
const gray = '#DEE0E0';
const white = '#FFF';

const themeVars = {
    "@primary-color": slate,
    "@body-background": gray,
    "@layout-header-background": gray
};

module.exports = {
    theme: themeVars
};